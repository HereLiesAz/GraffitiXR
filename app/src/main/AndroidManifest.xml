<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        The Meta wearables (mwdat-*) libraries declare minSdk 29, but the
        wearables flow is optional and gated at runtime (see WearableManager
        and ArViewModel.startGlassesSession). Override the merger so Android
        8.0+ devices can install the app; on SDK < 29 the wearables init path
        short-circuits to GlassesSessionState.Fallback.
    -->
    <uses-sdk tools:overrideLibrary="com.meta.wearable.dat.camera,com.meta.wearable.dat.mockdevice,com.meta.wearable.dat.core" />

    <!-- AR and Hardware Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />
    <uses-feature android:glEsVersion="0x00030000" android:required="false" />

    <!-- Storage and Location Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
                     android:usesPermissionFlags="neverForLocation"
                     tools:targetApi="33" />

    <!-- OTA Updater Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Permissions above (ACCESS_*_LOCATION, ACCESS_WIFI_STATE, etc.) would otherwise
        imply required hardware features and filter the Play Store listing. Mark them
        optional explicitly: the location/wifi flows are not core to the app, and core
        camera is the only required hardware.
    -->
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />

    <application
        android:name=".GraffitiApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GraffitiXR"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!--
            ARCore is optional: the app supports a non-AR experience (TRACE,
            MOCKUP, OVERLAY, STENCIL modes) on devices without ARCore. AR mode
            is gated at runtime via ArCoreApk.checkAvailability(). With value
            "optional" Google Play does not filter the listing to ARCore-eligible
            devices and does not auto-install the ARCore APK.
        -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <!--
             android:configChanges prevents the Activity from being destroyed
             and recreated during screen rotation, keeping the AR session alive.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode"
            android:theme="@style/Theme.GraffitiXR">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Callback scheme for Meta AI app redirection -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="graffitixr" />
            </intent-filter>

            <!-- Interop: receive an image shared from GraffiXR (e.g. an edited overlay returning from
                 the rich editor) and open it as an AR overlay layer. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!--
            ZXing's zxing-android-embedded ships a CaptureActivity declared with
            android:screenOrientation="sensorLandscape". From Android 16 the platform
            ignores orientation restrictions on large-screen devices (foldables, tablets),
            which Play Console flagged on release 14142. Both scanner invocations in the
            app already pass setOrientationLocked(false), so we can safely widen the
            merged entry to fullUser — respects the user's system rotation-lock setting
            on all form factors and satisfies the large-screen rule.
        -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullUser"
            tools:replace="android:screenOrientation" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <meta-data android:name="com.meta.wearable.mwdat.APPLICATION_ID" android:value="2122578028305600" />
        <meta-data android:name="com.meta.wearable.mwdat.CLIENT_TOKEN" android:value="AR|2122578028305600|0de697ded10d5ce4946e23f83a3fae7d" />
    </application>
</manifest>
